
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.HTMLШаблон) Тогда
		Объект.HTMLШаблон = "<HTML>";
		Объект.HTMLШаблон = Объект.HTMLШаблон + Символы.ПС + "<body>";
		Объект.HTMLШаблон = Объект.HTMLШаблон + Символы.ПС + "</body>";
		Объект.HTMLШаблон = Объект.HTMLШаблон + Символы.ПС + "</HTML>";
	КонецЕсли;
КонецПроцедуры
