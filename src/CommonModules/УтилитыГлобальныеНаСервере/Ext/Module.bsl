Функция БФТ_ФайлСуществует(ПутьКФайлу) Экспорт 
  Перем ПроверяемыйФайл;
  
  Если Не ЗначениеЗаполнено(ПутьКФайлу) Тогда
    Возврат Ложь;	
  КонецЕсли;
  
  ПроверяемыйФайл = Новый Файл(ПутьКФайлу);
  
  Возврат ПроверяемыйФайл.Существует();
КонецФункции
